---
description: English-only source code and comments; Arabic only in locales and user-facing UI strings
alwaysApply: true
---

# Language split — code vs UI

The dev team works in **English only** for everything in the repository except end-user Arabic copy.

## English only

- Source code identifiers (variables, functions, types, file names)
- **All comments** (`//`, `/* */`, JSDoc, block headers in `.ts`, `.tsx`, `.mjs`, `.go`, etc.)
- **Console / log / stderr** messages in scripts and Electron main (developer-facing)
- **Git commit messages** and PR descriptions
- **Cursor rules** and internal docs aimed at developers
- **`CHANGELOG.md`**, **`release-notes/*.md`**, and **GitHub Release descriptions**
- README and other repo documentation (technical prose in English)

## Arabic only

- `src/locales/ar.json`, `src/locales/currencies.ar.json`, and any future Arabic locale files
- User-visible strings in the React UI (via `t("…")` / i18n keys)

## User-facing errors

- Map thrown errors, IPC codes, and internal English messages with **`formatUiError(t, err)`** from `src/lib/uiErrors.ts` — never show raw `error.message` in the UI.
- Desktop notifications and alerts must use `t()` for title/body; details via `formatUiError`.

## Do not

- Mix Arabic into code comments or commit messages
- Hard-code Arabic UI strings in components when a translation key exists
- Add Arabic to CHANGELOG, release notes, commit messages, code comments, or script logs

## Examples

```typescript
// ❌ BAD — non-English comment (Arabic or other language in source)
/** … */

// ✅ GOOD
/** Unified accounts list with due progress bar */
```

```javascript
// ❌ BAD — non-English log message
console.log("[ship] …");

// ✅ GOOD
console.log("[ship] pushed to current branch");
```

```bash
# ❌ BAD — non-English commit message
git commit -m "…"

# ✅ GOOD
git commit -m "fix(home): reorganize stats into tabs"
```

When touching legacy Arabic comments or logs, translate them to English in the same change.
